GOOGLE_GENERATIVE_AI_API_KEY=your-api-key

# Vercel デプロイ時の DB（いずれか一方の連携を想定）
# --- Vercel Postgres（Storage）の場合 ---
# POSTGRES_URL=            # プール接続（通常クエリ用）
# POSTGRES_URL_NON_POOLING= # ダイレクト接続（init-db / DDL 用）
# --- Prisma Postgres（Marketplace）の場合 ---
# DATABASE_URL=   # プール接続（通常クエリ用）。連携で自動付与される
# DIRECT_URL=    # ダイレクト接続（init-db / マイグレーション用）。無い場合は DATABASE_URL を流用
